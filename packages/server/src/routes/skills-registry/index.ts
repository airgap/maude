/**
 * Skills Registry - Public API
 *
 * Re-exports the public interface that was previously exported
 * from the monolithic skills-registry.ts file.
 */

export { ensureSkillsTable } from './discovery';
export { skillsRegistryRoutes } from './registry';
